import React from 'react';

import { Container, HeaderTitle } from './styles';

const HeaderLogin: React.FC = () => {
  return (
    <Container>
      <HeaderTitle>Português (Brasil) </HeaderTitle>
    </Container>
  );
};

export default HeaderLogin;
